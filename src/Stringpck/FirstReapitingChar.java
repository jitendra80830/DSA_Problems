package Stringpck;

public class FirstReapitingChar {
    public static void main(String[] args){
        String str = "prepby";
        char ch = str.charAt(0);
        /*for (int i = 1; i <str.length() -1 ; i++) {
            if(ch == str.charAt(i)){
                System.out.print(str.charAt(i));
                break;
            }
            ch = str.charAt(i);
        }*/
    }
}
